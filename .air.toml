# .air.toml
root = "."
tmp_dir = "tmp"

[build]
cmd = "swag init -g cmd/server/main.go --parseDependency --parseInternal && go build -o ./tmp/main ./cmd/server"
bin = "tmp/main"
include_ext = ["go", "tpl", "tmpl", "html"]
exclude_dir = ["docs", "tmp"]
